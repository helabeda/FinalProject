{
  "name": "eventz",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd back && npm run back\" \"cd front && npm start\""
  },
  "author": "Ghorbell",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.3.0"
  }
}
